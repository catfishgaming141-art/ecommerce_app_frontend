import React from 'react';
import { View, Text } from 'react-native';

export default function Cart(){
  return (<View style={{flex:1,justifyContent:'center',alignItems:'center'}}><Text>Cart is a placeholder. Checkout flow in backend is available via /api/orders.</Text></View>);
}
